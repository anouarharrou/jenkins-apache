pipeline {
    agent any

    stages {
        stage('CHECKOUT_SCM') {
            steps {
                checkout scm
            }
        }

        stage('DEPLOY') {
            steps {
                echo "Hello!"
            }
        }
    }
}

